# ISEE Practice

A single-page ISEE practice app. No build step, no dependencies — everything
lives in `index.html`.

**Live:** https://nealweiner.github.io/isee-practice/

To run locally, just open `index.html` in a browser.
